syntax = "proto3";

import "UserTaskInfo.proto";

message UserGrowth {
    uint32 level_id = 1;
    int64 score = 2;
    int64 target_score = 3;
    double tmoney = 4;
    repeated UserTaskInfo task_info = 5;
}
