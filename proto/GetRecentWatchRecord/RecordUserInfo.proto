syntax = "proto3";

import "GetRecentWatchRecord/Tag.proto";
import "LiveThreadInfo.proto";

message RecordUserInfo {
    repeated Tag tags = 2;
    int32 is_follow = 4;
    int64 last_watch_time = 5;
    LiveThreadInfo thread_info = 6;
}
