syntax = "proto3";

import "ThreadInfo.proto";

message HeadLiveInfo {
    repeated ThreadInfo head_live = 1;
    repeated string head_pic = 2;
    string bitmap_wh_ratio = 3;
    int32 is_set = 4;
}
