syntax = "proto3";

import "ForcusRecommand.proto";
import "ThreadInfo.proto";

message DataRes {
    repeated ThreadInfo thread_list = 1;
    uint32 has_more = 2;
    uint32 pn = 3;
    ForcusRecommand forcus_recommand = 4;
}
